conda.enabled = true
        process.queue = 'cpuq'
        executor {
            name = 'slurm'
            queueSize = 350
            pollInterval = '5s' // Determines how often to check for process termination
        }

